/** Cash each player starts with in regular/full modes. */
export const STARTING_CASH = 8000;

/** Cash each player starts with in fast mode — lower for quicker, more tense games. */
export const FAST_STARTING_CASH = 4000;

/** Salary awarded on passing or landing on Ramses (classic only). */
export const RAMSES_SALARY = 800;
